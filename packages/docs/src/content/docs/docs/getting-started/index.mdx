---
title: Installation
description: How to install and set up feflow in your project
---

import { Button } from "feflow-svelte"
import { Tabs, TabItem } from "@astrojs/starlight/components"
import { Steps } from "@astrojs/starlight/components"
import { FileTree } from "@astrojs/starlight/components"

### Requirements

| Tooling       | Minimum Supported Version |
| ------------- | ------------------------- |
| **SvelteKit** | 2.x                       |
| **Svelte**    | 5.x                       |

<Steps>
    <ol>
        <li>
            ### Install `feflow-svelte`

            ```bash
            npm install feflow-svelte
            ```
        </li>

        <li>
            ### Setup provider

            Wrap your application with the `FeflowProvider` to enable all Feflow components.
            This should be done at the layout level to ensure global access.

            > Recommended: Place this in `src/routes/+layout.svelte` (SvelteKit root layout)

            <FileTree>
                - src
                    - routes
                        - +layout.svelte
            </FileTree>

            ```svelte
            <script lang="ts">
                import { FeflowProvider } from "feflow-svelte"
            </script>

            <FeflowProvider>
                {@render children()}
            </FeflowProvider>
            ```
        </li>

        <li>
            ### Enjoy!
            <Tabs>
                <TabItem label="Usage">
                    ```svelte
                    <script lang="ts">
                        import { Button } from "feflow-svelte"
                    </script>

                    <Button>
                        Click me
                    </Button>
                    ```
                </TabItem>
                <TabItem label="Preview">
                    <Button>
                        Click me
                    </Button>
                </TabItem>
            </Tabs>
        </li>
    </ol>

</Steps>
