---
title: Installation
description: How to install and set up navnex-kit in your project
---

import Button from "@/components/Button.vue"
import { Tabs, TabItem } from "@astrojs/starlight/components"
import { Steps } from "@astrojs/starlight/components"
import { FileTree } from "@astrojs/starlight/components"

<Steps>
    <ol>
        <li>
            ### Install
            <Tabs>
                <TabItem label="npm" icon="npm">
                    ```bash
                    npm install @navnex-kit/core @navnex-kit/vue
                    ```
                </TabItem>
                <TabItem label="pnpm" icon="pnpm">
                    ```bash
                    pnpm add @navnex-kit/core @navnex-kit/vue
                    ```
                </TabItem>
            </Tabs>
        </li>

        <li>
            ### Setup provider
            Wrap your application with the `NavnexProvider` to enable all navnex-kit components.
            This should be done at the layout level to ensure global access.

            > Recommended: Place this in `src/main.ts` (React root element)

            <FileTree>
                - src
                    - main.ts
            </FileTree>

            ```ts
            import { createApp, h } from "vue"
            import App from "./App.vue"
            import { NavnexProvider } from "@navnex-kit/vue"

            const app = createApp({
                render() {
                    return h(NavnexProvider, null, {
                        default: () => h(App)
                    })
                }
            })

            app.mount("#app")
            ```
        </li>

        <li>
            ### Enjoy!
            <Tabs>
                <TabItem label="Usage">
                    ```vue
                    <script setup lang="ts">
                        import { Button } from "@navnex-kit/vue"
                    </script>

                    <template>
                        <Button>
                            Click me
                        </Button>
                    </template>
                    ```
                </TabItem>
                <TabItem label="Preview">
                    <Button />
                </TabItem>
            </Tabs>
        </li>
    </ol>

</Steps>
