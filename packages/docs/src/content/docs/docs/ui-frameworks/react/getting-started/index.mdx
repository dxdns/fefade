---
title: Installation
description: How to install and set up Feflow UI your project
---

import { Button } from "@feflow-ui/react"
import { Tabs, TabItem } from "@astrojs/starlight/components"
import { Steps } from "@astrojs/starlight/components"
import { FileTree } from "@astrojs/starlight/components"

<Steps>
    <ol>
        <li>
            ### Install
            <Tabs>
                <TabItem label="npm" icon="npm">
                    ```bash
                    npm install @feflow-ui/core @feflow-ui/react
                    ```
                </TabItem>
                <TabItem label="pnpm" icon="pnpm">
                    ```bash
                    pnpm add @feflow-ui/core @feflow-ui/react
                    ```
                </TabItem>
            </Tabs>
        </li>

        <li>
            ### Setup provider
            Wrap your application with the `FeflowProvider` to enable all Feflow UI components.
            This should be done at the layout level to ensure global access.

            > Recommended: Place this in `src/main.tsx` (React root element)

            <FileTree>
                - src
                    - main.tsx
            </FileTree>

            ```tsx
            import { StrictMode } from "react"
            import { createRoot } from "react-dom/client"
            import App from "./App"
            import { FeflowProvider } from "@feflow-ui/react"

            createRoot(document.getElementById("root")!).render(
                <StrictMode>
                    <FeflowProvider>
                        <App />
                    </FeflowProvider>
                </StrictMode>
            )
            ```
        </li>

        <li>
            ### Enjoy!
            <Tabs>
                <TabItem label="Usage">
                    ```tsx
                    import { Button } from "@feflow-ui/react"

                    function App() {
                        return (
                            <>
                                <Button>
                                    Click me
                                </Button>
                            </>
                        )
                    }

                    export default App
                    ```
                </TabItem>
                <TabItem label="Preview">
                    <Button>
                        Click me
                    </Button>
                </TabItem>
            </Tabs>
        </li>
    </ol>

</Steps>
